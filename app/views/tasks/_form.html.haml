= simple_form_for([@task_list.user, @task_list, @task]) do |f|
  = f.error_notification
  = f.hidden_field :task_list_id, value: @task_list.id

  .form-inputs
    = f.input :name

  .form-actions
    = f.button :submit
